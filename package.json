{
  "name": "ces-json-server",
  "version": "1.0.0",
  "description": "CES Json Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch ./fakeapis/index.js --port 3000"
  },
  "keywords": [
    "json-server"
  ],
  "author": "Santhosh",
  "devDependencies": {
    "json-server": "^0.14.0"
  }
}
